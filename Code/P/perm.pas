program perm;
var arr:array[0..501]of char;
    tot,i,n:longint;
procedure permit(a:longint);
var
begin
    if a>n then
    exit;
	for i:=a to n do
	

end;
begin
    readln(n);
	for i:=1 to n do
	read(arr[i]);
	tot:=1;
	permit(1);
	writeln;
	writeln(tot);
end.